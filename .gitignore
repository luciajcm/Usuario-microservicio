# Ignore everything that is outside the immediate project folder (CC/Nuevo)
# This addresses the files starting with ../../ or ../../../
../../

# You might also want to ignore IDE/system files that often appear
# at the project level, though they may be fixed by the line above.
.vscode/

# Ignore temporary system files (like the ones with a ~$ prefix)
~$*

# Ignore compiled files or executables if your project creates any
*.exe
*.dll
*.o